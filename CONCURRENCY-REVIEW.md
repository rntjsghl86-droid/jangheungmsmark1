# 25명 동시 사용 검증

## 범위

관리자 5개, 일반 교원 20개 독립 쿠키 저장소로 로그인하여 실제 Next.js API 핸들러와 인증 코드를 실행했습니다. Supabase 입출력만 비동기 지연을 둔 메모리 어댑터로 대체했습니다. 운영 GitHub, Supabase, Vercel에는 변경을 보내지 않았습니다.

이 검사는 저장 충돌과 권한 분리에 대한 로컬 기능 검증입니다. 실제 브라우저 25대, 운영 DB, Vercel 실행 시간 제한, 네트워크 지연, 장기간 누적된 데이터 크기와 응답 속도는 검증하지 않았으므로 운영 환경의 25명 처리 성능을 보장하는 결과는 아닙니다.

## 요청한 일반 교원 권한

허용: 위반 기록 입력·수정·삭제, 학생 추가·수정·삭제, 리포트 내보내기.

제한: 전체 명단 교체·학년도 전환, 규정 설정, 위원회 처리, 권한 설정, 백업·복원.

기존 DB에 저장된 권한은 자동 변경하지 않았습니다. 현재 변경된 기본값은 로컬 미리보기에 반영되며, 운영 반영은 승인 후 별도 적용해야 합니다.

## 주요 결과

- 25개 세션이 10회에 걸쳐 기록 250건을 동시에 추가: ID 중복이나 누락 없음.
- 학생 25명 동시 추가, 서로 다른 학생 25명 동시 수정: 모든 변경 보존.
- 같은 기록 또는 학생을 25명이 동시에 수정: 1건 성공, 나머지 24건은 HTTP 409로 명시적 충돌 안내.
- 관리자 삭제와 일반 교원 수정 경합: 1건 성공, 1건 충돌 안내.
- 학생 삭제와 기록 추가 경합: 삭제된 학생을 참조하는 고아 기록 없음. 삭제가 나중에 저장된 경우 해당 학생 기록도 함께 삭제하는 기존 기능 의미를 유지함.
- 일반 교원 20명이 관리자 전용 기능에 접근: HTTP 403 차단.
- 관리자 5명이 동시에 권한 수정: 1건 성공, 나머지 4건 충돌 안내.
- 일반 교원의 저장 처리 도중 권한 회수: 재시도 시 새 권한으로 재검사하여 차단.
- 백업 복원 도중 다른 사용자가 저장: 오래된 백업 덮어쓰기 차단.
- 복원 완료 후 25개 이전 세대 요청: 모두 차단. 새로 읽은 상태에서 저장 가능.
- 오래된 전체 명단 저장으로 다른 교원의 최신 수정 덮어쓰기 차단.
- 관리자·교원 PIN 교차 사용 및 동일 PIN 설정 차단.
- 데이터 응답과 백업에서 PIN 해시 및 권한 설정 제외.

## 보완한 저장 방식

학생 명단 전체 자동 저장을 학생 단위 추가·수정·삭제로 바꾸었습니다. 수정과 삭제에는 편집 전 원본을 함께 보내 서버 최신 원본과 일치할 때만 저장합니다. DB 버전 비교 후 저장이 실패하면 최신 데이터를 다시 읽어 재시도하고 권한도 다시 검사합니다. 전체 명단 변경은 변경 전 데이터 비교를 요구합니다. 복원과 명단 교체는 데이터 세대를 변경하여 이전 세대에서 발송된 쓰기를 거부합니다.

## PIN

관리자는 ADMIN_PIN(숫자 4~12자리), 일반 교원은 SCHOOL_PIN 또는 기존 DB의 교원 PIN을 사용합니다. 서로 같은 PIN은 허용하지 않습니다. 관리자 PIN은 아직 실제 운영 환경에 설정하지 않았습니다. 이전 ADMIN_PASSWORD 환경변수는 관리자 로그인에 더 이상 사용하지 않습니다.

역할별 공용 PIN 방식입니다. 5명/20명은 동시 세션 수의 가정이며, 개인별 계정·개별 로그인 이력·퇴직자 한 명만의 접근 차단은 포함하지 않습니다.

## 재현

`node tests/concurrency-25.cjs` — 17개 동시 사용/인증 시나리오.

`node tests/security-backup.cjs` — 16개 기본 권한/백업 검증.

`npm run build` — 타입 검사와 프로덕션 빌드.

실행 결과는 `tests/concurrency-results.json`에 기록합니다. 기록된 시간은 로컬 가상 DB의 테스트 시간이며 운영 응답 시간으로 해석하면 안 됩니다.
