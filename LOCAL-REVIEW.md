# 로컬 검토 버전

GitHub 업로드, Supabase 변경, Vercel 배포는 사용자 승인 전까지 진행하지 않습니다.

## 실행

`npm install` 후 `npm run dev -- --hostname 127.0.0.1 --port 3100`을 실행하고 http://127.0.0.1:3100/preview 를 엽니다.

미리보기는 예시 데이터를 메모리에만 저장하며 Supabase에 연결하지 않습니다. 관리자/일반 교원 전환은 예시용이며 새로고침하면 데이터와 권한 설정이 초기화됩니다. 실제 로그인 페이지는 `/`입니다.

## 변경 사항

- 기존 교사 PIN 로그인은 일반 교원 역할을 사용하고, 관리자는 별도의 서버 ADMIN_PIN(숫자 4~12자리)으로 로그인합니다. 개인별 교원 계정 관리 기능은 포함하지 않습니다.
- 일반 교원은 기본적으로 조회, 위반 기록 입력·수정·삭제, 학생 추가·수정·삭제, 리포트 내보내기를 할 수 있습니다. 관리자가 추가 권한을 저장하며 서버 API에서도 권한을 검사합니다.
- 권한은 기존 school_app_state 행의 data.settings.teacherPermissions에 보관합니다. 새 테이블은 필요하지 않습니다.
- 데이터 응답과 백업에서 로그인 설정을 제외합니다.
- JSON 수동 백업과 파일 검증 후 복원을 지원합니다. 복원 전에 현재 데이터 다운로드를 시작합니다. 브라우저에서 다운로드한 파일의 실제 보관 여부를 확인해야 합니다.
- 백업 범위는 학생 명단, 위반 기록, 규정, 위원회 현황입니다. DB 전체, 권한, 로그인 설정, Supabase Auth, Storage는 포함하지 않습니다.
- 복원은 확인 문구와 동시 변경 검사를 거치며 기존 로그인 설정은 보존합니다.
- 명단 교체 및 진급 시 기록 초기화를 하나의 저장 요청으로 처리합니다.
- 브라우저 캐시의 예시 데이터를 운영 DB에 자동으로 입력하던 동작을 제거했습니다.

## 운영 적용 전 확인

현재 운영 DB 또는 비밀키는 읽거나 변경하지 않았습니다. 실제 Supabase 연동 테스트는 아직 하지 않았습니다. Vercel 환경변수 SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY(또는 SUPABASE_SECRET_KEY), SCHOOL_PIN, ADMIN_PIN가 필요합니다. 비밀키는 클라이언트 환경변수에 넣지 않습니다.

승인된 배포 전에 기존 데이터를 백업하고 실제 데이터 구조 호환성, school_app_state에 대한 직접 접근 RLS 정책, 두 브라우저에서의 권한/동시 저장을 확인해야 합니다. 기존 Next 및 xlsx 등 의존성의 보안 점검도 필요합니다.

검사: `node tests/security-backup.cjs`, `npx tsc --noEmit`, `npm run build`.
